#include <stdio.h>
/**
*main - starting point
*description:  the alphabet in lowercase
* Return: 0
*/
int main(void)
{
	char n;

	for (n = 'a' ; n <= 'z' ; n++)
	putchar(n);
	putchar('\n');
	return (0);
}
